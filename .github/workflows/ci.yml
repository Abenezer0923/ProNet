name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test-backend:
    name: Test Backend Services
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: profession_db_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install API Gateway dependencies
        working-directory: ./services/api-gateway
        run: npm install --legacy-peer-deps

      - name: Install User Service dependencies
        working-directory: ./services/user-service
        run: npm install --legacy-peer-deps

      - name: Build API Gateway
        working-directory: ./services/api-gateway
        run: npm run build

      - name: Create .env file for User Service
        working-directory: ./services/user-service
        run: |
          echo "CLOUDINARY_API_KEY=637454513411741" >> .env
          echo "CLOUDINARY_API_SECRET=7JHtL4bTAYqwq7RaQM60Ocf43fw" >> .env
          echo "CLOUDINARY_CLOUD_NAME=dgl4cpuik" >> .env
          echo "DAILY_API_KEY=d19964998b42867784f5ee7e9cf95ee4a05c044861b44aae14754eaa5fb47876" >> .env
          echo "DATABASE_HOST=dpg-d4askkpe2q1c73b80mrg-a" >> .env
          echo "DATABASE_NAME=profession_db" >> .env
          echo "DATABASE_PASSWORD=A7FSv3naAi5oIJkYlHKiTT5jcoLvkEla" >> .env
          echo "DATABASE_PORT=5432" >> .env
          echo "DATABASE_USER=profession_db_user" >> .env
          echo "EMAIL_PASSWORD=fzrlxrluiynrujpl" >> .env
          echo "EMAIL_USER=abenezerforjobs2@gmail.com" >> .env
          echo "FRONTEND_URL=https://pro-net-ten.vercel.app" >> .env
          echo "GOOGLE_CALLBACK_URL=https://pronet-user-service-qpe6.onrender.com/api/auth/google/callback" >> .env
          echo "GOOGLE_CLIENT_ID=716006181490-r1fvklb9s9b26nk7jdmf69vf315b9uaa.apps.googleusercontent.com" >> .env
          echo "GOOGLE_CLIENT_SECRET=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" >> .env
          echo "GOOGLE_REFRESH_TOKEN=1//03WvAwhk0kBmvCgYIARAAGAMSNwF-L9IrVrB4IjKBUMWr0aB0DviIeWaHGXXVjwRjCxOdTgdd0nS7dcZWnJJ4FSQT_C5VGSFo1Sk" >> .env
          echo "JWT_SECRET=\"VY2Lf3iix4PILDFNK7HggRhhHIiyQY3qB381Cq0Yui0=\"" >> .env
          echo "PORT=3001" >> .env
          echo "RESEND_API_KEY=re_4vTKv6Cz_HZ2iX37wBfEzcmntFxVS49Jt" >> .env
          echo "SMTP_HOST=smtp.gmail.com" >> .env
          echo "SMTP_PORT=587" >> .env
          echo "SMTP_SECURE=false" >> .env
          echo "STREAM_API_KEY=djqf3q9q4ygj" >> .env
          echo "STREAM_SECRET_KEY=rtv72856wrcq4946hv6srztkdgyqnh7xcrwnhse5r8f7aj7zhfp7ycnrkffmhfp5" >> .env

      - name: Build User Service
        working-directory: ./services/user-service
        run: npm run build

  test-frontend:
    name: Test Frontend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        working-directory: ./frontend
        run: npm install --legacy-peer-deps

      - name: Build frontend
        working-directory: ./frontend
        run: npm run build

  docker-build:
    name: Build Docker Images
    runs-on: ubuntu-latest
    needs: [test-backend, test-frontend]

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build API Gateway Image
        uses: docker/build-push-action@v4
        with:
          context: ./services/api-gateway
          push: false
          tags: pronet-api-gateway:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build User Service Image
        uses: docker/build-push-action@v4
        with:
          context: ./services/user-service
          push: false
          tags: pronet-user-service:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build Frontend Image
        uses: docker/build-push-action@v4
        with:
          context: ./frontend
          push: false
          tags: pronet-frontend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
