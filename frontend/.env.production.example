# Production Environment Variables for Vercel
# Copy these to Vercel Dashboard → Settings → Environment Variables

# API Gateway URL (for regular API calls)
# Set these to your Koyeb app URLs (or your custom domains).
# Example: https://pronet-api-gateway-<yourname>.koyeb.app
NEXT_PUBLIC_API_URL=https://pronet-api-gateway.<your-koyeb-subdomain>.koyeb.app

# WebSocket URL (for real-time features)
# Example: https://pronet-user-service-<yourname>.koyeb.app
NEXT_PUBLIC_WS_URL=https://pronet-user-service.<your-koyeb-subdomain>.koyeb.app

# Auth URL (for OAuth - goes directly to user service)
# If you use an API gateway that proxies auth, you can set this to NEXT_PUBLIC_API_URL instead.
NEXT_PUBLIC_AUTH_URL=https://pronet-user-service.<your-koyeb-subdomain>.koyeb.app
